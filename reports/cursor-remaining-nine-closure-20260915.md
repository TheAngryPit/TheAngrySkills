# Remaining Cursor mirror closure — 2026-09-15

This reviewed batch starts from merged PR #69 (`a4f9c236`) and closes the nine selected candidates that remained outside the published mirror. It publishes seven bounded Codex-native adaptations and retains two external-runtime holds. The source snapshot remains pinned to `cursor/plugins` commit `889ec4b68fa5aab0e867dad71ec3fdf386ae48f3`.

## Published adaptations

| Skill | Observable proof | Preserved limit |
| --- | --- | --- |
| `cursor-advisor` | Root spawned `/root/advisor_native_proof` as a requested `reviewer` on `gpt-6-astra` at low effort; it returned a bounded no-write verdict and the coordinator consumed it. | Requested model/role values are recorded; backend enforcement and automatic checkpoint hooks are not attested. |
| `cursor-check-agent-compatibility` | `codex-local-plugin-compatibility-v1` ran four independent static checks on a disposable plugin generated by the scaffold and returned local 100/100. | `agent_compatibility_score` stays `null`; no upstream scanner, target startup, component, hook, MCP, or runtime execution occurred. |
| `cursor-continual-learning` | A bounded memory curator reviewed the explicitly authorized transcript SHA-256 `e3c786a0a3f3aacff1ecb36c8f568cac05c3c9273ca112f7838631f127785d50` and produced proposal SHA-256 `4d510c0a0945b60590cad90b49b25db05cc01d7761d1ed8`. | Proposal only: no `AGENTS.md`, memory, repository, hook, runtime, or global-state write; automatic Stop triggering is unproven. |
| `cursor-create-plugin-scaffold` | A marked disposable project root generated static manifest, README, supplied license, rules, skills, agents, and commands, then entered the auditor. | Global `~/.cursor`, hooks, MCP, marketplace wiring, install, activation, and publication stop before writing or require separate review. |
| `cursor-review-plugin-submission` | The read-only structural auditor returned `STRUCTURAL_PASS` for the generated disposable scaffold and explicit findings for malformed, escaping, symlinked, hook, and MCP cases. | Structural pass is not marketplace acceptance; active/runtime/external behavior remains under the retained `needs_human_review` finding. |
| `cursor-ralph-loop` | Root resumed `/root/cancel_native_probe` twice through native follow-up. State advanced from iteration 1 SHA-256 `99ebd5bdd1ccdceb9f71db962f5266e303db17f5fbaa52a400819d85f4c89384` to iteration 2 SHA-256 `2690c4a30123ef7cc82689448198473a209b7ed19f9df454435e62be11137ffb`, stopping at promise `PROOF_DONE` and maximum 2. | Explicit coordinator-driven continuation only; no automatic Stop hook or unbounded loop claim. |
| `cursor-cancel-ralph` | Native interrupt on `/root/cancel_native_probe` returned prior status `running`; follow-up recovered the same exact child identity. | Cancellation requires an exact retained child identity; no recursive deletion or process-name termination. |

For these seven skills, the renderer now supports a reviewed `codex_native_body`. It preserves the pinned source and hash ledger as provenance while replacing the operational Cursor body in the published artifact. This prevents global Cursor paths, Cursor hooks, model defaults, and active external instructions from remaining below a warning banner.

## Retained holds

| Skill | Decision | Exact remaining dependency |
| --- | --- | --- |
| `cursor-cursor-sdk` | Held, `blocked_malicious` retained. | The safe non-executing capability map is useful evidence, but the mirrored upstream references still contain credential and MCP configuration surfaces. No Cursor SDK import, authentication, execution, streaming, or runtime equivalence was proved. |
| `cursor-orchestrate` | Held, `blocked_malicious` retained. | Root-local fan-out and drain were observed, but the selected ChatGPT Work cloud path still lacks an explicitly authorized Work root dispatch plus child tree, structured handoff, artifact identity, cancellation, and recovery proof. Local collaboration is not relabeled as Work cloud parity. |

## Delivery boundaries

The batch performs no merge, global installation, dependency installation, credential access, Work cloud task creation, marketplace submission, external message, or external publication beyond the explicitly authorized GitHub pull request. The root task sends proof, gaps, and actions not taken to the coordinating Codex task.

## Validation

- `pytest -q tests` — **266 passed, 2 subtests passed**.
- `python3 scripts/sync-cursor-plugin-skills.py --check` — **91 physical, 81 published, 387 output files**.
- `git diff --check` — passed.
- Deterministic local security scans ran on all seven newly published skills. Advisor, continual-learning, Ralph, and cancel-Ralph returned `safe_to_install` with zero findings. Compatibility, scaffold, and submission review returned `needs_human_review` only for bundled-script review or defensive mentions of hooks/MCP/plugin surfaces; there were no critical findings. Context review confirmed the three bundled adapters are bounded local readers/writers or static scanners, do not activate the named surfaces, and preserve the separate review gates.


## Security remediation

The first patch review identified and fixed three reachable gaps before commit: the scaffold now requires an explicit project root and rejects a fixture resolving outside it; the continual-learning mirror no longer bundles the upstream role that could write `AGENTS.md` and its index; and compatibility no longer distributes the `npx -y ...@latest` reviewer while its local scanner now bundles the required local submission auditor. Regression tests cover the symlinked-ancestor escape and execution of the published scanner bundle.
