{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "native-state-gated",
    "name_mapping": {
      "upstream_skill": "cancel-ralph",
      "published_name": "cursor-cancel-ralph",
      "source_path": "ralph-loop/skills/cancel-ralph/SKILL.md"
    },
    "preserved_semantics": "Cancel an active Ralph Loop. Use when the user wants to stop, cancel, or abort a running ralph loop.",
    "native_mapping": {
      "capabilities": [
        "project-local loop state reader"
      ],
      "required_tools": [
        "project-local loop state reader"
      ],
      "fallback": "No state or ambiguous path is a no-op that preserves the workspace.",
      "availability": "bounded_native_adaptation_observed_with_documented_limits"
    },
    "external_dependencies": [
      "project-local .cursor/ralph state"
    ],
    "permission_gates": [
      "Confirm exact project-local state; never use broad recursive delete or process-name kill.",
      "No state or ambiguous path is a no-op that preserves the workspace.",
      "Respect the current task scope, selected model/effort, repository ownership, and explicit approval requirements."
    ],
    "behavioral_proof": {
      "positive": "The native interrupt operation returned previous_status running for the exact child, and a follow-up recovered the same verified identity.",
      "missing_tool_or_input": "No active state returns INACTIVE; another session returns OTHER_SESSION and preserves state.",
      "error": "Symlinked state directories are rejected and no recursive deletion is used."
    },
    "promotion_status": "promoted_bounded_native_adaptation",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "ralph-loop/README.md",
      "url": "https://github.com/cursor/plugins/blob/889ec4b68fa5aab0e867dad71ec3fdf386ae48f3/ralph-loop/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "safe_docs_only",
      "status": "retained",
      "decision": "Keep the source finding and provenance; do not dismiss it because the adapter adds a warning."
    }
  },
  "codex_note": "## Native Codex boundary\n\nThe bundled [adapter](scripts/cursor_native_hook_adapters.py) cancels only matching project/session state; its synthetic fixture proves the file boundary but not a live hook state. A wrong or unknown session is a no-op. Never use the upstream recursive delete command. This candidate remains unindexed.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Cancel an active Ralph Loop. Use when the user wants to stop, cancel, or abort a running ralph loop.",
  "proof": "Pinned source and deterministic render plus synthetic CLI tests for task-bound state, continuation, promise, max, cancellation, wrong-session behavior, and symlinked project rejection. No project hook was installed or trusted; no live automatic continuation was observed.",
  "reason": "Per-skill Codex contract adds native mapping, permission gates, behavior proof, promotion state, security retention, and pinned README provenance without changing upstream source text.",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "1. Check if `.cursor/ralph/scratchpad.md` exists.\n\n2. **If it does not exist**: Tell the user \"No active Ralph loop found.\"\n\n3. **If it exists**:\n   - Read `.cursor/ralph/scratchpad.md` to get the current iteration from the `iteration:` field.\n   - Remove the state file and any done flag:\n     ```bash\n     rm -rf .cursor/ralph\n     ```\n   - Report: \"Cancelled Ralph loop (was at iteration N).\"\n",
      "after": "1. Use the bundled [native adapter](scripts/cursor_native_hook_adapters.py) with the exact project root and current session ID: `python3 scripts/cursor_native_hook_adapters.py ralph-cancel --project <project> --session-id <current-session-id>`.\n\n2. If it reports `INACTIVE`, tell the user no active Ralph loop was found. If it reports `OTHER_SESSION`, do not remove another task's state. If it reports `CANCELLED`, report the `iteration` returned by the task-bound adapter response.\n\n3. Do not use recursive deletion or process-name termination. The adapter removes only the state file for the matching session; a missing or ambiguous session leaves the project unchanged.\n"
    }
  ],
  "source_path": "ralph-loop/skills/cancel-ralph/SKILL.md",
  "source_sha256": "5824c5344af837c1cc56659d362124f75bdc1460ae66bf616e592fb2d8e8ea43",
  "codex_native_body": "# Cancel a native Codex Ralph Loop\n\nCancel only the exact active loop identified by its retained native child identity.\n\n## Workflow\n\n1. Read the bounded loop state and verify the project/task scope, child identity, current iteration, and active status.\n2. Call the native interrupt operation for that exact child. Do not terminate by process name or broad repository scope.\n3. Record the returned previous status and mark the loop cancelled. If the child was already complete or inactive, report that state without widening the action.\n4. Preserve unrelated state. Remove a local loop state file only when it belongs to the matching task and the removal is within the authorized cleanup scope.\n5. To recover after an accidental interruption, use a native follow-up on the same verified child identity and record the resumed state.\n\nThe observed proof interrupted `/root/cancel_native_probe` while running and then resumed that same identity through two bounded follow-ups. No recursive deletion, process-name kill, or automatic hook was used."
}
