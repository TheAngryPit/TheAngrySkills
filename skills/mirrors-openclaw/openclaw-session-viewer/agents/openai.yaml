interface:
  display_name: "Session Viewer"
  short_description: "Render session JSONL as searchable HTML"
  default_prompt: "Use $openclaw-session-viewer to render this agent session as searchable, shareable HTML."
