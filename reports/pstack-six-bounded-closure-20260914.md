# Remaining pstack bounded closure

Date: 2026-09-14. Scope: the six skills remaining after the first three
promotions. Every result below is explicit-only and bounded; none claims
automatic invocation, global installation, cloud parity, or production parity.

| Skill | Result | Concrete receipt | Still not claimed |
| --- | --- | --- | --- |
| `cursor-automate-me` | Promoted bounded explicit-only | `run_automate_me_fixture` and five focused tests require two evidence slices, corroborated preferences, project-local destination, explicit draft approval, readback, no-control text and no-symlink components. Missing history, unslop, or approval leaves the original unchanged. | Native task-history selection, native `skill-creator` telemetry, the complete user preference loop, and global writeback. |
| `cursor-figure-it-out` | Promoted bounded explicit-only | The renderer/decision-trail guard is verified, with adjacent isolated notes evidence for a falsifiable hypothesis loop: baseline, exact failure, trace, structural diff, reproduction-before-fix commit, temporary local remote and after-state. This is not a native `figure-it-out` invocation. | The overlay has only a partial post-change audit: no prospective trigger, full Phase A-E run, independently generated correction, real production target or independent review. |
| `cursor-poteto-mode` | Promoted bounded explicit-only; scripts gated | The core suite renders all 23 playbooks and exercises the isolated notes/process boundary. The coordinator-owned read-only audit (`5b8235f6`) found no load-time execution or hardcoded credential extraction, while retaining install, egress, authenticated GitHub, store/lock, watcher, cloud and PR gates. | Bundled script admission for unattended use, sticky mode, fresh profile loading, full live playbook execution, cloud lane, and real PR mutation. |
| `cursor-recall` | Promoted bounded explicit-only | `run_recall_fixture` selects only matching workspace/topic records, excludes the current task, reconciles live state and a matching shared record, returns a provenance-shaped brief, and performs no writes. Missing or mismatched exports are `PARTIAL`; four focused cases pass. Native task index/read was observed for the authorized `Echo Workflows V4` target, but its returned turn items were empty, so no transcript content was invented. | Full native task-history selection, transcript body readback, and complete `cursor-why` shared-record sweep. |
| `cursor-setup-pstack` | Promoted bounded explicit-only | The four fixture tests cover all 17 roles, four panel roles, aliases, unavailable model/effort and malformed input with no writes; the current-channel inventory records the advertised native model/effort palette. The generated route now has `allow_implicit_invocation: false`, checked alongside the other seven published pstack routes. | Persistent router/config write and readback, user-owned-task entitlement, parent alias resolution, and actual role dispatch. |
| `cursor-make-bot-ui` | Safe local variant only; security quarantine retained | Seven adapter tests prove local JSON POST shape, server-only key headers/redaction, nested secret rejection, local URL gating, one-attempt timeout/non-200 handling, constant timeout reason and payload-only failure evidence. | The current native surface has no configured capability for the real webhook routine, sender-key secret request, or wake turn; this is a capability gap, not only an authorization gap. Optional Tailnet exposure additionally needs Tailscale/network ingress state and explicit authorization, which are not a native capability in this task. No external request, secret, wake, Tailscale or sudo path ran. |

## Validation

The focused remaining-pstack/functional/core run passed 31 tests. The specific
figure/setup/remaining-adapter command passed 10 tests. The adapter and mirror
pytest selection passed 34 tests after the import-path fix. The generated
catalog check passed with 91 physical sources, 71 published mirrors and 345
output files. The full repository suite passed 254 tests and 2 subtests.
