{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "codex-cloud-cli-coordinator",
    "name_mapping": {
      "upstream_skill": "orchestrate",
      "published_name": "cursor-orchestrate",
      "source_path": "orchestrate/skills/orchestrate/SKILL.md"
    },
    "preserved_semantics": "Use only when the user explicitly types `/orchestrate <goal>` to decompose a large task, spawn a tree of parallel cloud-agent workers/subplanners/verifiers via the Cursor SDK, and collect structured handoffs; do not invoke autonomously.",
    "native_mapping": {
      "capabilities": [
        "bounded coordinator-owned dependency graph",
        "documented Codex Cloud task creation with codex cloud exec",
        "read-only task discovery, status and diff readback",
        "separately authorized cloud diff application"
      ],
      "required_tools": [
        "Codex CLI cloud commands",
        "one configured Codex Cloud environment ID"
      ],
      "optional_tools": [
        "codex cloud apply after separate authorization"
      ],
      "fallback": "Without a concrete environment ID, return ENVIRONMENT_REQUIRED and the validated plan; dispatch no tasks and do not retry the historical ChatGPT Work request.",
      "availability": "Codex CLI 0.154.0 and the Codex Cloud UI exposed exec/list/status/diff/apply plus UI cancellation. One authorized read-only audit ran to READY in environment 6aa9136795d0819186c67d8b812e7a70 with one attempt and zero changed lines; apply and cancellation were not used."
    },
    "external_dependencies": [
      "a configured Codex Cloud environment selected by exact environment ID",
      "same-account Codex CLI authentication already present on the host",
      "repository and branch available to the selected cloud environment"
    ],
    "permission_gates": [
      "Invoke only for an explicit /orchestrate <goal> request and preserve the user goal.",
      "Cloud task creation and transmission of goal/acceptance contents each require explicit authorization plus an exact environment ID.",
      "The adapter models one to four attempts and an optional branch; a fixed branch or commit may still be required by the workflow for reproducible proof.",
      "Apply requires separate authorization after the task status and diff are inspected.",
      "Do not treat local credential-pattern detection as proof that arbitrary prompt text is private-data-free.",
      "Do not execute the upstream Cursor runtime, install Bun dependencies, read credentials, contact Slack, or publish Git changes through this adapter.",
      "The CLI documents no cancellation command; the native task UI exposes cancellation, which remains a separate operator action."
    ],
    "behavioral_proof": {
      "positive": "Focused tests prove bounded DAG validation, dependency readiness derived only from validated PASS handoffs, one-to-four attempts, optional branch argv, separate task/content/apply authorization, real list JSON parsing, structured artifact identity, and exclusion of the upstream runtime. A single authorized cloud audit reached READY on commit 124c985348e79cec1a17cfc4ccd73e461e1efdc6 with one attempt and zero changed lines.",
      "missing_tool_or_input": "The adapter does not authenticate caller-supplied readback by itself, apply a diff, or expose CLI cancellation. Multi-task dependency drain, apply, and cancellation remain unproved in Codex Cloud.",
      "error": "Bare completed IDs, non-PASS or uninspected handoffs, invented textual artifact references, malformed digests, credential-shaped content, unsafe CLI identifiers, malformed task readback, graph cycles, unknown dependencies, excess children, unauthorized dispatch/content transmission, and unauthorized apply fail closed."
    },
    "promotion_status": "promoted_native_codex_cloud_adapter_single_read_only_runtime_proof",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "orchestrate/README.md",
      "url": "https://github.com/cursor/plugins/blob/889ec4b68fa5aab0e867dad71ec3fdf386ae48f3/orchestrate/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "blocked_malicious",
      "status": "retained_upstream_findings_excluded_from_published_bundle",
      "decision": "Retain every pinned upstream finding as provenance. Publish only source frontmatter, the rewritten Codex-native body, and the reviewed Python adapter; exclude all upstream prompts, references, schemas, TypeScript/Bun scripts, dependencies, measurement shell execution, Slack code, and credential paths.",
      "findings": [
        {
          "rule_id": "bundled-script-review",
          "severity": "warning",
          "path": "scripts/**",
          "line": null,
          "count": 62,
          "classification": "bundled_executable_surface",
          "effect": "The skill ships TypeScript/Bun scripts, tests, adapters, and CLI code that can execute local and external workflow actions.",
          "credentials": [
            "CURSOR_API_KEY",
            "SLACK_BOT_TOKEN"
          ],
          "decision": "Hold. Keep the bundled runtime external and require a separate code/security review before admission."
        },
        {
          "rule_id": "dependency-manifest-surface",
          "severity": "warning",
          "path": "scripts/package.json",
          "line": null,
          "classification": "active_dependency_surface",
          "effect": "The package manifest can install and run bundled dependencies.",
          "credentials": [],
          "decision": "Hold with the bundled runtime; do not install dependencies from this mirror."
        },
        {
          "rule_id": "persistence-mutation",
          "severity": "critical",
          "path": "scripts/measurements.ts",
          "line": 302,
          "classification": "active_executable_code_with_security_boundary",
          "effect": "runMeasurementCommand executes planner-authored bash in a worker branch and creates/removes a scratch HOME. The allowlist and cleanup are defensive controls, but the shell boundary is still active.",
          "credentials": [
            "allowlisted process environment"
          ],
          "decision": "Hold until the command boundary, environment allowlist, timeout, and cleanup receive separate review."
        },
        {
          "rule_id": "policy-bypass-instruction",
          "severity": "critical",
          "path": "scripts/__tests__/operator-boundary.test.ts",
          "line": 38,
          "classification": "defensive_test_reference",
          "effect": "The test asserts that ORCHESTRATE_OPERATOR is ignored without the home flag. The scanner matches the word \"ignores\" although the test enforces the policy gate.",
          "credentials": [],
          "decision": "Retain the finding as contextual/defensive, but hold the bundled runtime until the policy test and implementation are reviewed together."
        },
        {
          "rule_id": "persistence-mutation",
          "severity": "critical",
          "path": "scripts/adapters/slack/index.ts",
          "line": 51,
          "classification": "read_only_false_positive_context_with_external_adapter",
          "effect": "The flagged line only reads a Slack profile field; it is not persistence. The surrounding adapter still provides Slack API access and can create external messages elsewhere.",
          "credentials": [
            "SLACK_BOT_TOKEN"
          ],
          "decision": "Retain the scanner finding and hold the bundled Slack adapter pending external-egress and side-effect review."
        }
      ]
    }
  },
  "bundled_native_files": [
    {
      "source_path": "scripts/cursor_orchestrate_adapters.py",
      "target_path": "scripts/cursor_orchestrate_adapters.py"
    }
  ],
  "codex_native_body": "# Native Codex Cloud orchestration\n\nUse this mirror only when the user explicitly invokes `/orchestrate <goal>`. Preserve the goal, decompose it into a bounded coordinator-owned dependency graph, and use native Codex Cloud tasks only when an exact environment is configured and creation plus prompt-content transmission are authorized.\n\n## Workflow\n\n1. Validate the goal and task graph with `prepare_local_plan`. Keep exactly one planner, no cycles or unknown dependencies, and at most eight children.\n2. Select a concrete environment. `codex cloud list --json` may identify it by `environment_id`, `environment_label`, or both; an `All Environments` selector is not an environment ID.\n3. After explicit authorization for task creation and the goal/acceptance text sent to Codex Cloud, call `build_codex_cloud_dispatch_batch`. It returns shell-free argv for `codex cloud exec --env ENV_ID --attempts N [--branch BRANCH] QUERY`, with `N` from one through four; the adapter never executes the command. A workflow may still require a fixed branch or commit for reproducible proof.\n4. Submit only dependency-ready tasks. Inspect each with `codex cloud status TASK_ID` and `codex cloud diff TASK_ID`. A descendant unlocks only after a structured `PASS` handoff includes cloud task and attempt identity, status and diff inspection, and an artifact URI plus SHA-256 digest.\n5. Applying a cloud diff is a separate mutation. Build `codex cloud apply TASK_ID --attempt ATTEMPT` only after separate authorization.\n6. The CLI documentation exposes no cancellation command. The native Codex Cloud task UI does expose cancellation; using it is a separate operator action. Stopping dispatch only prevents new tasks and does not cancel an existing task.\n\n## Boundaries\n\nGoal and acceptance text are copied into the cloud prompt. Explicit content authorization is the controlling boundary. Local private-path and high-confidence credential-pattern checks are limited defenses and cannot establish that arbitrary text is safe to transmit. Do not send secrets, private inputs, customer data, proprietary code, personal data, or internal URLs without authorization for that exact destination and purpose.\n\nCodex Cloud coordination preserves planner/worker/verifier intent without reproducing Cursor's private internal agent tree. Keep the pinned upstream `blocked_malicious` findings as provenance. Do not publish or execute the upstream TypeScript/Bun runtime, dependency manifest, measurement shell boundary, Slack adapter, or credential paths. Do not treat the earlier provisional ChatGPT Work client ID as a Codex Cloud task or retry it. If there is no concrete `ENV_ID`, return `ENVIRONMENT_REQUIRED` with the validated plan and do not dispatch.\n",
  "codex_note": "## Codex Cloud runtime proof and retained historical Work state\n\nOn 2026-09-15, the existing GitHub installation already covered all repositories, so no repository-access mutation was needed. Direct search found `TheAngryPit/TheAngrySkills`. One environment (`6aa9136795d0819186c67d8b812e7a70`) was created with the universal image, automatic setup, no environment variables or secrets, and internet off. Exactly one authorized read-only audit (`task_e_6aa91395baf483268a13e1a5cd9db403`) ran on `main` commit `124c985348e79cec1a17cfc4ccd73e461e1efdc6`, reached `READY`, used one attempt, and changed zero files or lines. No diff was available and none was applied. The CLI documents no cancellation command, while the task UI exposed a cancellation button that was not used.\n\nThe real list payload used `cursor` and returned a null `environment_id` with a valid `environment_label`; the adapter now accepts and tests that shape. The cloud audit identified readiness, native exec-shape, and content-boundary gaps that are addressed by the current adapter and tests. The earlier ChatGPT Work client ID remains separate with unknown state and must not be retried. See `reports/cursor-orchestrate-codex-cloud-closure-20260915.md`.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Use only when the user explicitly types `/orchestrate <goal>` to decompose a large task, spawn a tree of parallel cloud-agent workers/subplanners/verifiers via the Cursor SDK, and collect structured handoffs; do not invoke autonomously.",
  "proof": "Pinned source hash and deterministic generation prove provenance. Focused tests prove the native adapter contracts. One real, fixed-commit Codex Cloud audit reached READY with one attempt and zero changed lines, and its list payload provided the runtime regression fixture. This proves one read-only task path; it does not prove multi-task drain, apply, or cancellation.",
  "reason": "Preserve explicit-only orchestration semantics through documented Codex Cloud commands while excluding the unsafe upstream runtime and removing literal Cursor internal-tree parity as an admission requirement.",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "description: Use only when the user explicitly types `/orchestrate <goal>` to decompose a large task, spawn a tree of parallel cloud-agent workers/subplanners/verifiers via the Cursor SDK, and collect structured handoffs; do not invoke autonomously.",
      "after": "description: \"Use only when the user explicitly types `/orchestrate <goal>` to coordinate a bounded dependency graph through native Codex Cloud tasks; requires an exact configured environment and authorization for task creation.\""
    }
  ],
  "source_path": "orchestrate/skills/orchestrate/SKILL.md",
  "source_sha256": "a56718f3acf4bbc13e44ca7b8e00d499e818fb16ee293bbe3d58621968c28e22",
  "retained_source_files": [
    "SKILL.md"
  ]
}
