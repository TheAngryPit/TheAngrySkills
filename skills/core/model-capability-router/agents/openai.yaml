interface:
  display_name: "Model Capability Router"
  short_description: "Route work across Codex subagents and user-owned tasks"
  default_prompt: "Use $model-capability-router to select a channel-aware model, effort, role, lifecycle, capability flow, fallback, and proof bar."
