{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "delegated-native",
    "name_mapping": {
      "upstream_skill": "check-agent-compatibility",
      "published_name": "cursor-check-agent-compatibility",
      "source_path": "agent-compatibility/skills/check-agent-compatibility/SKILL.md"
    },
    "preserved_semantics": "Run four separate repository checks: deterministic scanner, cold startup, validation loop, and docs reliability; combine their evidence into one compatibility score and prioritized fixes.",
    "native_mapping": {
      "capabilities": [
        "native bounded delegation",
        "repository command runner",
        "documentation reader",
        "deterministic local Codex scanner"
      ],
      "required_tools": [
        "repository command runner",
        "documentation reader"
      ],
      "fallback": "If native delegates are unavailable, execute the four checks sequentially with separate evidence and the same score contract. If the deterministic scanner cannot run, report it as unavailable and do not invent an Agent Compatibility Score.",
      "availability": "bounded_native_adaptation_observed_with_documented_limits",
      "optional_tools": [
        "native bounded delegation"
      ]
    },
    "external_dependencies": [
      "agent-compatibility npm scanner package and network/package trust review",
      "target repository startup and validation prerequisites",
      "native bounded delegation when available"
    ],
    "permission_gates": [
      "Keep scanner, startup, validation, and docs checks separate; each needs its own evidence and score.",
      "Use only the bundled deterministic local scanner. Do not execute the upstream npx latest-package command from the pinned source roles.",
      "If the deterministic scanner is unavailable, identify the environment gap and do not invent a combined score.",
      "Preserve native model/effort and request read-only reviewer behavior on the actual delegate surface; frontmatter alone does not enforce permissions."
    ],
    "behavioral_proof": {
      "positive": "Pinned renderer bundles four role references and corrects invalid upstream frontmatter in an isolated held preview; no repository scan or startup result is claimed.",
      "missing_tool_or_input": "Scanner unavailable or missing startup prerequisites must be reported as environment gaps; the four-check score is withheld until real evidence exists.",
      "error": "A failed scanner or startup command must retain raw failure evidence and cannot be converted into a fabricated low repository score."
    },
    "promotion_status": "promoted_bounded_native_adaptation",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "agent-compatibility/README.md",
      "url": "https://github.com/cursor/plugins/blob/889ec4b68fa5aab0e867dad71ec3fdf386ae48f3/agent-compatibility/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "safe_docs_only",
      "status": "retained",
      "decision": "Keep the source finding and provenance; do not dismiss it because the adapter adds a warning."
    }
  },
  "codex_note": "## Codex adaptation boundary\n\nThe pinned source description has an unquoted colon; this preview quotes it for native parsing. The four source reviewers are bundled under `references/` as role instructions with Cursor `model: fast` and `readonly: true` frontmatter removed. Use four separate bounded native delegates when available, or four separate sequential checks, and request read-only behavior through the actual host permission surface. The scanner role names `npx -y agent-compatibility@latest`; review package version, installation effects, and egress before execution. Do not compute a combined score unless the deterministic scanner and all three behavioral checks produced real evidence. This candidate remains unindexed.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Run four separate repository checks: deterministic scanner, cold startup, validation loop, and docs reliability; combine their evidence into one compatibility score and prioritized fixes.",
  "proof": "Pinned source hashes and renderer verify the corrected frontmatter without distributing the unsafe upstream role commands. The local Codex scanner runs four independent static checks on a generated disposable plugin and returns 100/100 with separate role evidence; the upstream scanner score, target cold startup, and runtime checks remain unproven.",
  "reason": "The pinned upstream description contains an unquoted colon and fails YAML parsing. The held Codex preview quotes that scalar and bundles all four named reviewer roles without activating their commands.",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "description: Run the full repository compatibility pass: scanner score, startup path, validation loop, and docs reliability.",
      "after": "description: \"Run the full repository compatibility pass: scanner score, startup path, validation loop, and docs reliability.\""
    }
  ],
  "source_path": "agent-compatibility/skills/check-agent-compatibility/SKILL.md",
  "source_sha256": "2391a06aec1bfb475690a9c386207e009090e35c624b5ca6cace3ea4c6705970",
  "codex_native_body": "# Check agent compatibility in Codex\n\nRun a bounded, evidence-based compatibility review of an explicitly supplied local plugin directory.\n\n## Workflow\n\n1. Run `python3 scripts/cursor_plugin_scanner_adapters.py compatibility <plugin-root> --local-scan`.\n2. Preserve the four separate results: deterministic structural scan, startup-contract review, validation-contract review, and documentation reliability review.\n3. Report `local_codex_compatibility_score` as the local static Codex score. Never label it as the upstream Agent Compatibility Score; `agent_compatibility_score` remains unavailable unless the upstream scanner was separately reviewed and executed.\n4. Treat `runtime_executed: false` and `components_executed: false` as material limits. Verify actual startup and validation behavior separately when the user requests runtime proof and the repository provides safe commands.\n5. Prioritize fixes from observed issues. Do not penalize the target for unavailable external tooling.\n\nThe local scanner never executes plugin components, hooks, MCP servers, startup commands, or tests. It performs no network access, credential reads, or external writes.",
  "bundled_native_files": [
    {
      "source_path": "scripts/cursor_plugin_scanner_adapters.py",
      "target_path": "scripts/cursor_plugin_scanner_adapters.py"
    },
    {
      "source_path": "scripts/cursor_plugin_submission_audit.py",
      "target_path": "scripts/cursor_plugin_submission_audit.py"
    }
  ]
}
