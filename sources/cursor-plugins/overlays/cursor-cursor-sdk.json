{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "bounded-native-codex-migration-adapter",
    "name_mapping": {
      "upstream_skill": "cursor-sdk",
      "published_name": "cursor-cursor-sdk",
      "source_path": "cursor-sdk/skills/cursor-sdk/SKILL.md"
    },
    "preserved_semantics": "Guide users building apps, scripts, CI pipelines, or automations on top of the Cursor TypeScript SDK (`@cursor/sdk`). Use this skill whenever the user mentions integrating, installing, or writing code against the Cursor SDK; whenever they say `Agent.create`, `Agent.prompt`, `Agent.resume`, `agent.send`, `run.stream`, `CursorAgentError`, or `@cursor/sdk`; whenever they ask to run Cursor agents programmatically from a script, CI/CD pipeline, GitHub Action, backend service, or any other code that isn't the Cursor IDE itself; and whenever they want to pick between local and cloud runtime, configure MCP servers for an SDK agent, or handle streaming, cancellation, or errors from an SDK agent. Also trigger when a user is wiring Cursor into an automation, writing a bot that runs Cursor, or porting REST `/v1/agents` calls to the SDK, even if they don't explicitly name the package. Use this eagerly rather than answering from memory; the SDK surface evolves and this skill plus its references are the source of truth for the external package.",
    "native_mapping": {
      "capabilities": [
        "bounded Cursor SDK request reader",
        "native Codex migration planner",
        "caller-supplied native Codex bridge"
      ],
      "required_tools": [
        "local Python adapter"
      ],
      "optional_tools": [
        "native Codex bridge for an explicitly authorized read-only operation"
      ],
      "fallback": "Without a native bridge, return the bounded operation plan and UNAVAILABLE; never substitute Cursor SDK execution or configure MCP.",
      "availability": "bounded native migration adapter test-proven; actual native task execution and Cursor SDK runtime unproven"
    },
    "external_dependencies": [
      "@cursor/sdk",
      "CURSOR_API_KEY",
      "Cursor REST/SDK",
      "MCP configuration"
    ],
    "permission_gates": [
      "Keep Cursor SDK runtime and references/auth.md plus references/mcp.md external; do not install, execute, authenticate, or expose SDK credentials from the mirror.",
      "Use only the bounded adapter and a caller-supplied native Codex bridge after the coordinator establishes authorization outside the request payload; native task creation remains an explicit operator action.",
      "The Cursor SDK and native connector/tool permissions are not proven in Codex.",
      "Do not suppress or rewrite the upstream scanner findings to make the flow appear safe."
    ],
    "behavioral_proof": {
      "positive": "The bounded adapter translates prompt, resume, wait, and stream requests into native Codex tool operations; a read-only caller-supplied bridge returns ordered wait/readback evidence without importing Cursor or writing externally.",
      "missing_tool_or_input": "Missing native bridge returns UNAVAILABLE; missing bridge methods return MISSING_CAPABILITY; missing thread or bounded input returns a validation error without dispatch.",
      "error": "Credential or MCP-shaped request/result data returns FAIL_CLOSED before dispatch; bridge exceptions and error statuses return NATIVE_ERROR without echoing provider messages or secrets."
    },
    "promotion_status": "promoted_bounded_native_migration_adapter_task_execution_unproven",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "cursor-sdk/README.md",
      "url": "https://github.com/cursor/plugins/blob/c1c0a32802223f4be824112dd83d33ad29a8b26c/cursor-sdk/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "blocked_malicious",
      "status": "retained_upstream_findings_excluded_from_published_bundle",
      "decision": "The pinned upstream blocked_malicious verdict remains source provenance. Publish only the rewritten Codex-native body, source frontmatter, and reviewed adapter; exclude every upstream reference file from the rendered bundle.",
      "findings": [
        {
          "rule_id": "credential-path",
          "severity": "critical",
          "path": "references/auth.md",
          "line": 100,
          "classification": "contextual_documentation_text",
          "effect": "The upstream auth guide describes CURSOR_API_KEY handling for the external SDK.",
          "credentials": [
            "CURSOR_API_KEY"
          ],
          "decision": "Retain the finding; the native adapter never reads, accepts, or publishes the key."
        },
        {
          "rule_id": "mcp-plugin-hook-install",
          "severity": "critical",
          "path": "references/mcp.md",
          "line": null,
          "classification": "active_configuration_reference",
          "effect": "The upstream MCP guide describes configuring MCP servers and auth injection for the external SDK.",
          "credentials": [
            "MCP authentication"
          ],
          "decision": "Retain the finding; MCP-shaped request, bridge, or result data fails closed and no MCP server is configured."
        }
      ]
    }
  },
  "bundled_native_files": [
    {
      "source_path": "scripts/cursor_sdk_native_adapter.py",
      "target_path": "scripts/cursor_sdk_native_adapter.py"
    }
  ],
  "codex_native_body": "# Bounded Cursor SDK migration for Codex\n\nUse this mirror when the request concerns `@cursor/sdk`, `Agent.create`, `Agent.prompt`, `Agent.resume`, `agent.send`, `run.stream`, `run.wait`, `CursorAgentError`, local/cloud selection, or MCP configuration. Preserve the upstream SDK guidance as external reference while using the local migration adapter for the Codex path.\n\n## Workflow\n\n1. Convert the requested operation to a JSON object with `operation` (`prompt`, `create`, `resume`, `send`, `wait`, or `stream`) and only the required safe fields. Do not include `CURSOR_API_KEY`, `apiKey`, headers, environment credentials, MCP servers, or auth material.\n2. Run `python3 scripts/cursor_sdk_native_adapter.py plan` with the JSON request on stdin. The adapter emits the closest native Codex operations (`create_thread`, `send_message_to_thread`, `wait_threads`, and bounded `read_thread`) and keeps `run.stream` as a no-streaming-equivalent wait/readback pair.\n3. For a read-only native operation, the coordinator may supply an already-authorized native Codex bridge to `run_native_migration(..., execute=True)`. The bridge is external to this script; its result is validated and never treated as Cursor SDK parity.\n4. A missing bridge is `UNAVAILABLE`, and a missing bridge method is `MISSING_CAPABILITY`. A mutating native task operation requires explicit authorization and is never created by the CLI or this mirror.\n5. Report native bridge errors as `NATIVE_ERROR` without copying provider messages, paths, credentials, or MCP configuration. A credential/MCP-shaped request or result is `FAIL_CLOSED`.\n\n## Security and provenance\n\nThe pinned upstream scanner verdict `blocked_malicious` remains retained. Keep the concrete `references/auth.md:100` credential-path finding and MCP hook/configuration findings visible in review records. Do not publish or copy those reference files into a native mirror, read `CURSOR_API_KEY`, install `@cursor/sdk`, import or execute the Cursor runtime, activate MCP, or infer cloud/local equivalence. The native adapter proves only its bounded translation and caller-supplied bridge contract; actual Cursor SDK behavior and native Codex task execution require a separate authorized runtime pass.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Guide users building apps, scripts, CI pipelines, or automations on top of the Cursor TypeScript SDK (`@cursor/sdk`). Use this skill whenever the user mentions integrating, installing, or writing code against the Cursor SDK; whenever they say `Agent.create`, `Agent.prompt`, `Agent.resume`, `agent.send`, `run.stream`, `CursorAgentError`, or `@cursor/sdk`; whenever they ask to run Cursor agents programmatically from a script, CI/CD pipeline, GitHub Action, backend service, or any other code that isn't the Cursor IDE itself; and whenever they want to pick between local and cloud runtime, configure MCP servers for an SDK agent, or handle streaming, cancellation, or errors from an SDK agent. Also trigger when a user is wiring Cursor into an automation, writing a bot that runs Cursor, or porting REST `/v1/agents` calls to the SDK, even if they don't explicitly name the package. Use this eagerly rather than answering from memory; the SDK surface evolves and this skill plus its references are the source of truth for the external package.",
  "proof": "Pinned source hash and deterministic build are static provenance. Focused SDK adapter tests prove bounded prompt/stream translation, ordered wait/readback through a caller-supplied read-only bridge, unavailable and missing-capability results, explicit mutation authorization, credential/MCP fail-closed behavior, and redacted bridge errors. Cursor SDK import/auth/streaming/runtime parity and native Codex task execution remain unproven.",
  "reason": "Preserve the upstream SDK trigger and blocked_malicious findings while replacing unsafe operational instructions with a functional, bounded Codex migration bridge.",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "description: Guide users building apps, scripts, CI pipelines, or automations on top of the Cursor TypeScript SDK (`@cursor/sdk`). Use this skill whenever the user mentions integrating, installing, or writing code against the Cursor SDK; whenever they say `Agent.create`, `Agent.prompt`, `Agent.resume`, `agent.send`, `run.stream`, `CursorAgentError`, or `@cursor/sdk`; whenever they ask to run Cursor agents programmatically from a script, CI/CD pipeline, GitHub Action, backend service, or any other code that isn't the Cursor IDE itself; and whenever they want to pick between local and cloud runtime, configure MCP servers for an SDK agent, or handle streaming, cancellation, or errors from an SDK agent. Also trigger when a user is wiring Cursor into an automation, writing a bot that runs Cursor, or porting REST `/v1/agents` calls to the SDK, even if they don't explicitly name the package. Use this eagerly rather than answering from memory; the SDK surface evolves and this skill plus its references are the source of truth for the external package.",
      "after": "description: \"Use when building or troubleshooting an integration with the external Cursor TypeScript SDK (@cursor/sdk), including Agent.create, Agent.prompt, Agent.resume, streaming, MCP, local or cloud runs, and error handling.\""
    }
  ],
  "source_path": "cursor-sdk/skills/cursor-sdk/SKILL.md",
  "source_sha256": "3fbe439f366ea94e0a756fc7288d2d8bd3f871bed4b97219dc1aee2f8c8ab979",
  "retained_source_files": [
    "SKILL.md"
  ]
}
