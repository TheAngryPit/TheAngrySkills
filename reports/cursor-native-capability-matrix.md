# Cursor → Codex capability proof matrix

State of PR #64 on 2026-09-14. A source hash or rendered
instruction is provenance, not execution proof. `held` means the skill is in
the 88-skill active inventory but outside the published skill catalog.

| Capability | Evidence in this branch | Remaining gap / decision |
| --- | --- | --- |
| Upstream catalog and source dependencies | Manifest pins 91 physical skills (88 active, 3 dormant), 27 plugin-level support files, and the native hook adapter by SHA-256. `sync-cursor-plugin-skills.py --check` reports 91 physical, 59 published, 239 output files after the bounded `swarm` promotion. | 29 active candidates are held. Publication of the other 59 does not by itself prove runtime parity. |
| Explicit-only invocation policy | The [Cursor skill contract](https://prod.cursor.com/docs/skills) says `disable-model-invocation: true` prevents automatic selection while retaining explicit invocation. The [Codex skill contract](https://learn.chatgpt.com/docs/build-skills) maps this to `agents/openai.yaml` with `policy.allow_implicit_invocation: false`. The renderer removes the Cursor-only frontmatter key and emits native policy for all 39 published skills with that source flag; the held preview emits it for 14 more. Tests cover the full source-to-output set. | This establishes a source-faithful generated policy, not live explicit or implicit invocation. The separate native discovery task must test runtime behavior. |
| Installed pstack 47-skill comparison | [Per-skill audit](pstack-47-skill-audit-20260914.md) maps every top-level trigger, workflow, dependency, agent/model route, output, and gap. Installed cache and pin share all 47 IDs; 45 top-level skill bodies match byte-for-byte. Two skills and six nested playbooks differ; 27 cache-only support files are separated from the pin. | Installed cache says 0.15.1, but the skills-only pin lacks plugin release metadata. Three installed delegate defaults differ from the pin. No pin update or native activation follows from the comparison. |
| pstack playbooks and named companion skills | Held `cursor-poteto-mode` preview renders 23 playbooks and names `cursor-deslop`, `cursor-control-cli`, `cursor-control-ui`, model-capability-router, proof-orchestrator, and the public native `poteto-agent` profile. `cursor-no-comments` names `comment-sicko`; routed `how`, `why`, `interrogate`, `reflect`, and `swarm` retain their own roles. [Profile matrix](../docs/native-agent-profiles.md) records invocation and result ownership. The two source-grounded TOMLs match installed bytes. The current native `collaboration.spawn_agent` surface exposed both named agent types and returned bounded synthetic probes; `collaboration.followup_task` resumed the same poteto-agent path for a second observed Bun result: [runtime evidence](cursor-native-profile-live-probe-20260914.md). | No fresh top-level task profile selection, raw injected-instruction readback, complete role compliance, automatic skill trigger, or sticky mode proof. The Comment Sicko first-output marker was not visible in its returned final text. `model-capability-router` was unavailable as an installed skill to the poteto probe. Explicit follow-up reuse here does not prove automatic cross-turn routing. |
| Project-local skill discovery and invocation | The [official Codex contract](https://learn.chatgpt.com/docs/build-skills) documents `.agents/skills` project discovery, explicit `$name`, implicit description selection, and `agents/openai.yaml` with `allow_implicit_invocation: false`. A disposable Git fixture contained explicit-only and implicit-eligible synthetic skills plus a byte-identical `model-capability-router`. A Python scanner checked the three files and policy and classified name-only prompts. A fresh native project worktree based on a commit containing the fixture listed the implicit-eligible skill and router in its session catalog: [native probe](cursor-native-project-skill-discovery-20260914.md). | The explicit-only skill was absent from that catalog; the cause and explicit-selection path were not observable. The literal `$name` sent through the task API did not supply a native selection token or skill-read trace. An isolated `codex exec` returned `401 Unauthorized` before a model response. Explicit, implicit, and delegated-agent activation, full runtime skill reads, and router use remain unobserved. No global home or installation was changed. |
| `how` / `why` bounded native roles | A real `--preview-candidates` case used a native explorer, then a simple-path explainer with all five pinned sections. Separate native Git, Linear, Notion and Slack investigators and a synthesizer produced the pinned `why` evidence/confidence structure. A bounded missing-module/dependency run reported explicit unknown/error without widening; unavailable connectors and forge auth were named. [Role-flow report](pstack-how-why-real-20260914.md). | Both are now published as explicit-only, bounded read-only workflows. Automatic skill selection, the complex `how` branch, complete seven-source `why` access, and live parity remain unobserved. The source connector-security finding is retained with a reviewed no-write/native-permission decision in the `why` overlay. |
| `bug-fix` workflow | A synthetic notes app captures create/search, exact `not-found` failure, trace, and pass after a prewritten correction. It records a structural diff, commits local reproduction before correction, and pushes only to a temporary bare remote. The negative fixture refuses correction on a different failure. [Fixture report](../.scratch/pstack-bug-fix-notes/implementation-report.md). | `FIXTURE_ONLY`. Portions of source steps 1, 4, and 5 are exercised locally; 2, 3, and 6 are partial. No full `how`/`why` binary search, independent architect/delegation/review, real PR, or autonomously generated correction. |
| Project verification skills | A disposable notes CLI fixture runs each mapped action as a separate bounded Python subprocess, records exit code/stdout/stderr and declared regular-file state, then writes project-local `.agents/skills/verify-notes/` with Launch, Doctor, Drive, Evidence, Cleanup, Helpers, feature map, and surviving JSON evidence. A controlled feature drift is reconciled and a second unchanged pass is clean. `README` feature slugs are rejected before writing. | `FIXTURE_ONLY`: the generated Doctor checks only file existence/non-symlink, not Python syntax, version, or app health. No live target, source-wave delegation per feature, browser/Electron control, changed-outcome PR, native skill activation, or full maintenance pass is proven. Filesystem/network isolation was not observed. |
| `show-me-your-work` local trail | The published explicit-only skill keeps the pinned source intact and adapts its writer to require an explicit task/workspace root. Fourteen behavior tests cover append, formula/newline sanitization, invalid roots/escapes, symlink/hardlink/FIFO rejection, 24 complete parallel rows under an exclusive file lock, and refusal of evident credential-shaped cells before log mutation. A real append-only trail was audited against this task history and reviewed independently by `gpt-6-astra`: [proof and Attention](cursor-show-work-review-20260914.md). | The credential guard is heuristic. Crash recovery, uncooperative writers, `fsync` durability, automatic skill selection, and other-host behavior remain unproven. The writer requires Python 3 plus host `fcntl`, `O_NOFOLLOW`, `O_DIRECTORY` and `O_NONBLOCK`; native filesystem permissions apply to each run. |
| `thermos` two-lens native review | Two independent read-only native reviewers received the same PR #64 diff and distinct pinned bug/security and code-quality rubrics, returned separate `ISSUES` findings, and the coordinator deduplicated one verdict. A second case interrupted one reviewer and reported `PARTIAL` single-lens without a double-review claim: [case report](pstack-thermos-swarm-real-20260914.md). | Published as a bounded explicit-only review. The original `ISSUES` findings and missing-lens gap remain visible; automatic selection, malformed-diff behavior, and other hosts are unobserved. |
| `swarm` four-phase native local review | A [visible checklist](pstack-swarm-four-phase-20260914.md) was written before N=2 Luna-high local subagents launched on disjoint PR #64 slices. Both returned `PASS` with cited evidence; coordinator drained both and reported one aggregate. A prior case retained `ISSUES`/`BLOCKED` worker outcomes with a `BLOCKED` aggregate. | Published only for the bounded local-computer path, with explicit-only policy. Default Work cloud workers are still unavailable without a proved worker route; race selection, dropout, automatic trigger, and other-host behavior are unobserved. |
| `setup-pstack` model mapping | A dry-run fixture covers all 17 pinned role labels and four panel roles with caller-supplied model/effort inventory. Four tests preserve explicit selections, panel counts and `inherit-parent`/`auto`, reject missing model/effort or malformed panels, and refuse an alias effort when the parent model is unknown. A [current-channel pass](cursor-setup-pstack-native-inventory-20260914.md) reads this task's Sol-medium turn context, the advertised native subagent model/effort palette, and the standing router to propose all role choices. | The fixture remains dry-run; advertised subagent models are not proof of user-owned-task/Work cloud entitlement, actual role dispatch, parent alias resolution or persistent router write/readback. The installed router is behind the source's profile-distribution section. `cursor-setup-pstack` remains held. |
| Writing and TypeScript companion skills | Published `cursor-technical-writing` now requires the published `cursor-unslop` for every touched document, as the source does. Published `cursor-typescript-best-practices` names both `cursor-principle-type-system-discipline` and `cursor-principle-boundary-discipline` and retains bundled `references/patterns.md`. Generated-output tests cover the links. | These are corrected instruction links, not a live native trigger or combined writing/TypeScript workflow proof. |
| Official Cursor runtime contract | [Subagents](https://cursor.com/docs/subagents) document isolated local context, background/resume, inherited local MCP tools, and readonly write restrictions. [Cloud Agents API](https://cursor.com/docs/cloud-agent/api/endpoints) distinguishes agents/runs, repository refs, branches, artifacts and terminal cancellation. [Automations help](https://cursor.com/help/ai-features/automations) confirms an HTTP POST webhook trigger. | The pinned `why` claim that readonly removes MCP is a source assumption, not an official platform contract; its overlay now avoids granting write access to preserve MCP. Automation docs do not establish pstack's Bearer/`X-Automation-Key`, timeout, retry or response rules for `make-bot-ui`; the separate [Origin signed webhook](https://cursor.com/docs/api/origin) is a different protocol. Codex equivalence remains unproven. |
| Operator-selected ChatGPT Work cloud for `orchestrate` | [Native contract map](cursor-orchestrate-work-cloud-contract-20260914.md) relates explicit root task creation to `create_thread(target.type: chatgptWorkCloud)` and existing `list_threads`/`read_thread` surfaces. The pinned Cursor source still requires a planner tree, structured handoffs, git-backed state, artifacts, cancellation and recovery. | A read-only Work task creation attempt was rejected by automatic approval review because this specific external task lacked explicit trusted user authorization; no task was created. The available Work surface does not document child fan-out/drain, branch artifacts or cancellation. The candidate remains held and no local/Cursor-script fallback is labeled cloud parity. |
| pstack bundled scripts | [Static and scratch runtime review](../.scratch/pstack-bug-fix-notes/implementation-report.md) identifies Bun install, local store/locks, `git`/`gt`/`gh` subprocesses, `git fetch`, `gh api graphql`, and transcript-home reads. Exact lock pins installed in scratch with scripts disabled; a fresh cache-populated bootstrap installed and restarted. `orch` CLI changed a disposable store, `watch-pr` CLI parsed mocks and made one bounded read-only status query of PR #64, 52 bundled tests and typecheck passed, and a slow mock was stopped by an outer timeout. Node also ran `check-plan.mjs`; Bash ran `worktree-audit.sh` with mocks. | Polling/stack paths, frontier subprocesses, and live product behavior remain unproven. The bootstrap key does not verify the installed package version. The cache run was not network-instrumented; this is a limit of the proof, not a new gate. |
| `watch-pr` required review gate | The real PR #64 reported `REVIEW_REQUIRED`; the pinned source displayed Review ✅ because that cell only checked threads. A hash-checked held preview now renders `review required`, blocks non-draft `REVIEW_REQUIRED` as a merge gate, and retains `null` as a distinct no-decision state. Synthetic regression and a second read-only status query of PR #64 passed. | The preview is not distributed or trusted as a live Codex skill. PR #64 is now non-draft and remains `REVIEW_REQUIRED`; the non-draft merge gate is proven by the synthetic fixture, not by a live `watch-pr` run. |
| Ralph loop and cancel | Held previews bundle the hash-checked native adapter. Synthetic `Stop` payload tests cover exact promise, max iteration, duplicate turn, corrupt state, and task/project scoped cancellation. | No trusted `.codex/hooks.json`, live Stop event, or real loop continuation. |
| Advisor | Held preview bundles pinned role and native adapter. Synthetic payload tests cover known `apply_patch` marker, expected agent/verdict, one-time nudge, and explicit task-bound enable/status/expect/disable without default model. | No live read-only advisor consult or proved ordering for expected ID versus `SubagentStop`; shell/MCP/out-of-band edits are not sensed. |
| Continual learning | Held preview bundles pinned updater role and native adapter. Synthetic Stop tests cover threshold, transcript root/mtime, duplicate turn; a bounded synthetic updater pass changed only fixture AGENTS/index and its second unchanged-mtime pass was a no-op. | No real transcript, trusted hook, automatic update, or global memory write proof. |
| Agent compatibility | Held preview repairs pinned invalid YAML description and bundles four named reviewer roles; strict frontmatter audit has zero errors. | Published `agent-compatibility` scanner is not installed locally. Package/version/installation/egress review and real scanner, startup, validation, docs checks are absent; no combined score is claimed. |
| Documentation Canvas | The pinned `cursor-docs-canvas` source explicitly calls its own body a placeholder and requires `~/.cursor/skills-cursor/canvas/SKILL.md` plus Cursor Canvas SDK declarations. The candidate remains held. | No pinned Canvas SDK or proved native Codex canvas primitive maps that component/hook contract. A linked-document fallback is only a declared path, not Canvas parity. |
| Cursor plugin submission audit | Held preview bundles a hash-checked read-only structural auditor. Synthetic fixtures check manifest JSON, bounded component paths, frontmatter field presence, README, and passive hook/MCP detection. | Full YAML validity, marketplace source/name uniqueness, documentation quality, runtime behavior, and submission policy remain unproven. Source `needs_human_review` finding and `security_hold_*` promotion status remain intact; no recommendation to publish. |
| Cursor plugin scaffold | Held preview bundles a hash-checked static scaffold fixture plus pinned plugin-architect and quality-gate references. A marked disposable root produces a Cursor-format manifest, README, caller-supplied LICENSE and four static component examples; the held auditor returns `STRUCTURAL_PASS`. Hooks, `mcpServers`, and marketplace requests stop before writing. | The pinned source still defaults to `~/.cursor/plugins/local/`; the preview explicitly warns against following it. This is not a native Codex plugin installer, global installation, active component, or publication proof. Source security hold remains. |
| External service, cloud, hooks/MCP, and unsafe install surfaces | Manifest and per-skill overlays retain contextual scanner findings for xAI voice requests, Cursor SDK credential/MCP references, orchestrate scripts/Slack, create-plugin hooks/MCP, make-bot-ui install, and pstack scripts. | Held until each named service, secret, permission, side effect, and native host capability is reviewed and exercised. No global install, successful cloud task creation, secret submission, hook trust, or merge was performed. |

Validation for the state above: `pytest -q tests` passed 197 tests and 2
subtests; bundled Bun tests passed 52/52 and strict typecheck passed in scratch;
`sync-cursor-plugin-skills.py --check` reports 91/59/239, and
`git diff --check origin/main` passed. GitHub `Validate skill stack` completed
successfully for published head `ab038461` ([job receipt](https://github.com/TheAngryPit/TheAngrySkills/actions/runs/34860841957/job/104032320133)).
