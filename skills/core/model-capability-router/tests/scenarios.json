{
  "scenario": "Route Codex work across master, field coordinator, native subagent, and native task lanes",
  "red": {
    "observed_failures": [
      "treated a model listed in TOML as spawnable on every channel",
      "attempted to launch Luna through spawn_agent",
      "collapsed the Sol master orchestrator and Terra field coordinator into one role",
      "pinned every worker and prevented vanilla Codex selection",
      "presented user-owned create_thread tasks as native child subagents"
    ]
  },
  "green": {
    "automated_proof": [
      "schema 2 channel-aware presets validate",
      "master orchestration resolves to current-task Sol Medium",
      "planning resolves only to native-subagent Sol High",
      "vanilla subagent routing emits no model, effort, or role pin",
      "field coordination resolves to a reusable Terra task with a creation gate",
      "Luna routes require the create_thread channel",
      "ephemeral Luna tasks cannot be reused",
      "missing native controls, role mismatches, and missing skills block"
    ]
  }
}
