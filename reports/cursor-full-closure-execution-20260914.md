# Cursor full-catalog closure execution ledger

Date: 2026-09-14. This is the execution queue after PR #66, not a claim that
publication, native activation, global installation, and full source parity are
the same event. The pinned input has 91 physical skills: 88 active slash skills
and three Benny automation sources. PR #66 is the historical 61-skill base; this
isolated successor branch adds bounded `cursor-interrogate`, `cursor-reflect`,
`cursor-create-verification-skill`, `cursor-maintain-verification-skill`,
`cursor-no-comments`, `cursor-automate-me`, `cursor-figure-it-out`,
`cursor-poteto-mode`, `cursor-recall`, and `cursor-setup-pstack`, reaching 71
published and 17 active held. Benny is a separate automation pack and is not
silently subtracted from the physical inventory.

## Closure rule

For each active skill, read the pinned skill and all relative references,
preserve its trigger and output, map its exact native dependencies, and run at
least one bounded success and one relevant degraded/error path. Publish only
the branch whose behavior has actually been observed. An unavailable provider,
Canvas SDK, cloud tree, trusted hook, or model readback remains an explicit
capability result inside an otherwise useful conditional adapter; it does not
become an invented success. Retain each original scanner finding. Require
deterministic generator output, source/license provenance, explicit-only policy
where the source disables model invocation, baseline-comparative security
scan, public-safe diff, tests, and CI before PR review. No global install,
automation activation, external communication, or merge follows from a mirror.

## Pstack first: nine active closure batch

| Batch | Skills | Owner and next reviewable result | Dependency or honest limit |
| --- | --- | --- | --- |
| Native model/role routing | `cursor-setup-pstack`, `cursor-poteto-mode`, `cursor-no-comments` | All three are bounded explicit-only promotions: setup role mapping remains dry-run, Poteto guidance is admitted with bundled scripts gated, and no-comments keeps the named `comment-sicko` loop. | Preserve `model-capability-router` authority. Persistent dispatch, fresh profile loading and exact model readback remain unproven. |
| Context and learning | `cursor-figure-it-out`, `cursor-recall`, `cursor-automate-me` | All three have bounded explicit-only receipts: isolated hypothesis loop, scoped history/shared-record reconciliation, and approved project-local preference draft. | Native trigger/history/skill-creator telemetry, production target and global writeback remain explicit gaps. |
| User verification | `cursor-create-verification-skill`, `cursor-maintain-verification-skill` | Both are bounded explicit-only promotions backed by the existing CLI/UI receipts, source-wave evidence, cleanup ownership, persisted Doctor evidence, feature map, and clean second run. | Production target parity, host activation and action-time UI Reset remain separate gaps; no corrective PR when there is no proven defect. |
| External bot UI | `cursor-make-bot-ui` | Sol: local mock webhook UI with server-only key boundary and negative malformed/timeout paths; retain installer quarantine. The current native surface lacks the real webhook-routine, sender-key secret-request and wake capability; optional Tailnet exposure additionally needs Tailscale/network-ingress state. | No real provider request, secret, privileged Tailscale installer, or public exposure without specific authorization and review. |

## Other active skills: 16 held

| Batch | Skills | Owner and next reviewable result | Dependency or honest limit |
| --- | --- | --- | --- |
| Canvas/document surfaces | `cursor-docs-canvas`, `cursor-pr-review-canvas-pr-review-canvas` | Luna high `canvas_pair`: functional bounded linked-document/PR artifact and negative path, with exact Canvas parity gap. | Cursor Canvas SDK is absent from the pinned skills-only input; local artifact is a conditional output, not Canvas execution. |
| Voice | `cursor-add-dictation`, `cursor-add-read-aloud`, `cursor-add-voice`, `cursor-debug-voice` | Luna high `voice_conditional`: local mocked STT/TTS/realtime/log behavior with safe server credential boundary and error paths. | Provider credentials, microphone/audio routing, network, and production playback stay unavailable until separately proven. |
| Hooks and loops | `cursor-advisor`, `cursor-continual-learning`, `cursor-ralph-loop`, `cursor-cancel-ralph` | Sol: task-bound local hook/state fixtures, idempotency/cancellation and live readback only after project hook trust is authorized. | A synthetic `Stop` payload does not prove native automatic continuation or safe transcript access. |
| Plugin and scanner | `cursor-check-agent-compatibility`, `cursor-create-plugin-scaffold`, `cursor-review-plugin-submission`, `cursor-cursor-sdk` | Sol: pinned package/scanner audit; functional static scaffold and complete read-only submission checks in a disposable project; SDK conditional/no-credential path. | Preserve source scanner holds. Do not execute active hooks/MCP, SDK auth, global install, or marketplace publication as a static test. |
| History and cloud | `cursor-workflow-from-chats`, `cursor-orchestrate` | Sol: exact parent/subagent history/redaction fixture and native Work root/child/artifact contract when authorized. | The available Work surface has no proven child fan-out/drain/cancel/recovery parity. A local worker tree is not Work cloud. |

## Benny automation pack: three physical sources outside the 88

`cursor-reproduce-and-fix-issues`, `cursor-setup-benny`, and
`cursor-triage-issue-reports` remain retained under
`pstack/automations/benny`. Luna high `benny_automation_pack` owns their
source/side-effect inventory and a separate conditional native design or
disposable proof. They are neither installed slash skills nor activated
automations. Scheduling, issue triage mutations, PR creation, and external
notifications each need their own authority and proof.

## Active permissions and sequencing

The coordinator is consolidating the exact UI fixture Reset and Work cloud
questions. Work that does not depend on those answers proceeds now. The PR #66
review requirement remains intact and does not block isolated successor
branches. Results from the independent batches enter the shared catalog only
after Sol reviews their source fidelity, proof level, security findings and
generated output; workers own their bounded files, not publication authority.
