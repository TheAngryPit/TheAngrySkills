{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "delegated-native",
    "name_mapping": {
      "upstream_skill": "swarm",
      "published_name": "cursor-swarm",
      "source_path": "pstack/skills/swarm/SKILL.md"
    },
    "preserved_semantics": "Fan out N parallel workers, drain them, and return one report. Use for /swarm, 'swarm this', or parallel coverage, races, gauntlets, and exploration.",
    "native_mapping": {
      "capabilities": [
        "bounded worker delegates",
        "local result collector"
      ],
      "required_tools": [
        "bounded worker delegates",
        "local result collector"
      ],
      "fallback": "A partial worker is marked PARTIAL/ISSUES and is not silently dropped.",
      "availability": "not_proven_in_current_codex_session"
    },
    "external_dependencies": [
      "native bounded delegates if exposed"
    ],
    "permission_gates": [
      "Use an explicit worker count and disjoint scope; sequential fallback is not fan-out proof.",
      "A partial worker is marked PARTIAL/ISSUES and is not silently dropped.",
      "Respect the current task scope, selected model/effort, repository ownership, and explicit approval requirements."
    ],
    "behavioral_proof": {
      "positive": "Positive fixture for cursor-swarm: the bounded path produces its documented result without unrelated side effects.",
      "missing_tool_or_input": "Missing-capability fixture for cursor-swarm: A partial worker is marked PARTIAL/ISSUES and is not silently dropped.",
      "error": "Error fixture for cursor-swarm: a malformed input or failed dependency is reported explicitly and does not widen scope."
    },
    "promotion_status": "held_until_behavior_proof_and_permission_gate",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "pstack/README.md",
      "url": "https://github.com/cursor/plugins/blob/889ec4b68fa5aab0e867dad71ec3fdf386ae48f3/pstack/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "safe_docs_only",
      "status": "retained",
      "decision": "Keep the source finding and provenance; do not dismiss it because the adapter adds a warning."
    }
  },
  "codex_note": "## Codex adaptation\n\nUse an explicit worker count and disjoint scope; sequential fallback is not fan-out proof. Required native capabilities: bounded worker delegates, local result collector. External dependencies remain explicit: native bounded delegates if exposed. A partial worker is marked PARTIAL/ISSUES and is not silently dropped. This candidate remains unindexed until the stated behavior and permission proof exists.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Fan out N parallel workers, drain them, and return one report. Use for /swarm, 'swarm this', or parallel coverage, races, gauntlets, and exploration.",
  "proof": "Pinned source hash and deterministic build are static provenance only. The Codex contract records positive, unavailable-capability/input, and error cases.",
  "reason": "Per-skill Codex contract adds native mapping, permission gates, behavior proof, promotion state, security retention, and pinned README provenance without changing upstream source text.",
  "replacements": [],
  "source_path": "pstack/skills/swarm/SKILL.md",
  "source_sha256": "d578d877c3b8b9de7163f52579c4683337a940acc0fbe4496a498f5d3b27df30"
}
