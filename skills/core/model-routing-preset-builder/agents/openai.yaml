interface:
  display_name: "Native Routing Policy Builder"
  short_description: "Five native profiles without mandatory routing stages"
  default_prompt: "Use $model-routing-preset-builder for the requested native model-routing decision, respecting the selected main model and current channel availability."
