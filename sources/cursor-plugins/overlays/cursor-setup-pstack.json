{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "native-config-gated",
    "invocation_policy": "explicit_only",
    "name_mapping": {
      "upstream_skill": "setup-pstack",
      "published_name": "cursor-setup-pstack",
      "source_path": "pstack/skills/setup-pstack/SKILL.md"
    },
    "preserved_semantics": "Configure which models pstack uses per role and at what reasoning budget. Detects your available models and writes an always-applied rule that overrides the skill defaults. Use for /setup-pstack, \"configure pstack models\", \"pstack budget\", or changing pstack's model choices.",
    "native_mapping": {
      "capabilities": [
        "native model inventory",
        "native reasoning effort inventory",
        "bounded budget mapping",
        "local config diff"
      ],
      "required_tools": [
        "native model inventory",
        "native reasoning effort inventory",
        "bounded budget mapping",
        "local config diff"
      ],
      "fallback": "Unavailable model or declined approval leaves configuration unchanged.",
      "availability": "bounded_dry_run_budget_mapping_and_native_inventory_observed; persistent role dispatch remains unproven"
    },
    "external_dependencies": [
      "Codex model inventory",
      "project/global config destination"
    ],
    "permission_gates": [
      "Dry-run first; write only to an explicit project-local destination after approval.",
      "Unavailable model or declined approval leaves configuration unchanged.",
      "Respect the current task scope, selected model/effort, repository ownership, and explicit approval requirements."
    ],
    "behavioral_proof": {
      "positive": "Positive fixture for cursor-setup-pstack: all roles, panel counts, aliases, and a selected reasoning budget map deterministically without unrelated side effects.",
      "missing_tool_or_input": "Missing-capability fixture for cursor-setup-pstack: an unavailable model, effort, or budget target leaves configuration unchanged and identifies the role needing a choice.",
      "error": "Error fixture for cursor-setup-pstack: an unknown budget, malformed panel, or failed dependency is reported explicitly and does not widen scope."
    },
    "promotion_status": "promoted_bounded_explicit_only_budget_mapping_persistent_write_and_dispatch_unproven",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "pstack/README.md",
      "url": "https://github.com/cursor/plugins/blob/c1c0a32802223f4be824112dd83d33ad29a8b26c/pstack/README.md",
      "sha256": "cd63df9733eb5b393371baca8194ea2dd9a3c815803ac48b8ff6580e49adef71",
      "note": "Verified at the reviewed upstream head; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "safe_docs_only",
      "status": "retained",
      "decision": "Keep the source finding and provenance; do not dismiss it because the adapter adds a warning."
    }
  },
  "codex_note": "## Codex runtime mapping\n\nThe standing `model-capability-router` is the single policy owner. Read reports/cursor-setup-pstack-native-inventory-20260914.md for the current subagent-channel inventory and 17-role/four-panel candidate map, then read reports/cursor-setup-pstack-upstream-adaptation-20260916.md for the reviewed budget delta. Current-task, subagent, user-owned task and Work cloud channels differ. This route is explicit-only because persistent model configuration requires a deliberate request. The bounded adapter maps budgets to native efforts and same-family detected model variants; it never writes Cursor rules or changes the active task. The current inventory and fixture do not prove a write, new-session selection, parent alias resolution, or full pstack execution.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Configure which models pstack uses per role and at what reasoning budget. Detects your available models and writes an always-applied rule that overrides the skill defaults. Use for /setup-pstack, \"configure pstack models\", \"pstack budget\", or changing pstack's model choices.",
  "proof": "The reviewed upstream skill, README, and setup guide are pinned by exact head hashes and the deterministic build. Dry-run fixtures cover 17 labels/four panels, all four budget labels, same-family model fallback, aliases, unavailable model/effort/budget targets, and malformed input without writes. The current native subagent inventory and standing router remain the source of available model/effort choices. Persistent write/readback, task-channel entitlement, parent alias resolution, actual role dispatch, and full pstack execution remain unproved.",
  "reason": "Preserve pstack role and reasoning-budget configuration using canonical Codex model policy and exact-channel capability instead of Cursor-specific rules; require explicit invocation before model configuration.",
  "codex_native_body": "# Setup pstack for Codex\n\nConfigure effective pstack model and reasoning-budget choices using native Codex selection and the standing `model-capability-router`. Do not write `~/.cursor/rules/pstack-models.mdc`, invent Cursor slugs, or switch the active task model. This skill responds to an explicit model-setup request.\n\n## Steps\n\n### 1. Detect native capability\n\nRead the models and efforts advertised for the exact Codex channel being configured. Current task, subagent, user-owned task, and Work cloud channels may differ. Record availability only when that channel accepts it.\n\n### 2. Read canonical policy\n\nRead the installed `model-capability-router` and its canonical TheAngrySkills source when a persistent change is requested. Respect the operator's current model/effort and explicit selections. The router owns routing and total-cost policy; this skill supplies pstack role and budget questions, not a second default table.\n\n### 3. Choose a budget and map roles\n\nOffer these exact labels: `unlimited — keep max`, `large — xhigh reasoning`, `medium — high reasoning`, and `small — medium reasoning`. On a re-run, preserve role families, panel lists, and aliases (`inherit-parent`, `auto`). Apply the selected target effort (`max`, `xhigh`, `high`, or `medium`) to each real model, choosing the highest supported effort at or below the target. If the detected inventory uses a different slug for the same family, select that detected variant; if no supported variant exists, mark the role as needing a choice. Aliases remain unchanged. Cover every upstream role: feature/refactoring, bug-fix, perf-issue, hillclimb, judgment/prose, hardest tasks, how explorer/explainer, why investigators/synthesizer, reflect tooling/judgment/divergent/synthesizer, arena runners/cross-judge pool, swarm workers, architect runners, and interrogate reviewers. Panel list length still controls fan-out.\n\n### 4. Validate\n\nValidate every real model and effort against the exact channel inventory. A missing model, effort, or budget target is a bounded block; keep configuration unchanged and report the role needing a choice. `inherit-parent` and `auto` omit the model override only when the native channel supports inheritance.\n\n### 5. Persist when requested\n\nFor an authorized persistent change, update the canonical `model-capability-router` source or its conditional pstack role reference with a reviewed diff and affected routing fixtures. A task-local selection stays in the task brief and handoffs. If source checkout or write authority is unavailable, return the mapping and exact blocked write; never claim persistence.\n\n### 6. Read back\n\nAfter an authorized persistent update, run router checks and verify the source diff. If installation is separately authorized and performed, read back the installed skill and effective native config. A source edit or fixture-only mapping does not prove runtime selection, new-session behavior, parent alias resolution, or full pstack execution.\n\n### 7. Offer verification\n\nIf the project has no way to drive the real app for proof, offer once to create a project-local verification skill. Invoke the installed `cursor-create-verification-skill` by exact name only when available and explicitly requested; otherwise report that dependency rather than claiming it ran.\n",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "name: cursor-setup-pstack\n",
      "after": "name: cursor-setup-pstack\ndisable-model-invocation: true\n"
    }
  ],
  "source_path": "pstack/skills/setup-pstack/SKILL.md",
  "source_sha256": "2399a5670c6408e92a996299c9de9f22736997cb7e835c694309e7c6fc8c39bf"
}
