interface:
  display_name: "Native Model Routing"
  short_description: "Five native profiles without mandatory routing stages"
  default_prompt: "Use $model-capability-router for the requested native model-routing decision, respecting the selected main model and current channel availability."
