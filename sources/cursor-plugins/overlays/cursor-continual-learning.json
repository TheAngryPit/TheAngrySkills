{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "delegated-write-gated",
    "name_mapping": {
      "upstream_skill": "continual-learning",
      "published_name": "cursor-continual-learning",
      "source_path": "continual-learning/skills/continual-learning/SKILL.md"
    },
    "preserved_semantics": "Orchestrate continual learning by delegating transcript mining and AGENTS.md updates to `agents-memory-updater`.",
    "native_mapping": {
      "capabilities": [
        "bounded transcript reader",
        "AGENTS.md diff"
      ],
      "required_tools": [
        "bounded transcript reader",
        "AGENTS.md diff"
      ],
      "fallback": "A missing transcript/updater returns a proposed-only result with no write.",
      "availability": "bounded_native_adaptation_observed_with_documented_limits"
    },
    "external_dependencies": [
      "scoped transcript source"
    ],
    "permission_gates": [
      "Run only on an explicit user trigger or an operator-enabled trusted project workflow.",
      "Limit review to verified transcripts for the current workspace and task; exclude secrets and unrelated private conversations.",
      "The delegate is proposal-only and may not edit AGENTS.md, memory, repository instructions, indexes, hooks, or runtime state without separate authorization.",
      "No native delegate or verified transcript source means report not-run with no write."
    ],
    "behavioral_proof": {
      "positive": "A bounded native memory curator reviewed one explicitly authorized transcript, returned a hashed proposal, and performed no AGENTS.md, memory, repository, hook, runtime, or global-state write.",
      "missing_tool_or_input": "Missing scoped transcript or delegate must return not-run with no write; native fallback is not runtime exercised.",
      "error": "Malformed hook state and wrong transcript root fail closed in the synthetic hook fixture; updater errors are not yet exercised."
    },
    "promotion_status": "promoted_bounded_native_adaptation",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "continual-learning/README.md",
      "url": "https://github.com/cursor/plugins/blob/c1c0a32802223f4be824112dd83d33ad29a8b26c/continual-learning/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "safe_docs_only",
      "status": "retained",
      "decision": "Keep the source finding and provenance; do not dismiss it because the adapter adds a warning."
    }
  },
  "codex_note": "## Codex adaptation and boundary\n\nUse this only for an explicit request to mine prior chats or for an operator-enabled, trusted project hook. The pinned `agents-memory-updater` is bundled as [a native role reference](references/agents-memory-updater.md). Delegate the full transcript/index/AGENTS.md flow to that bounded role, using the operator-selected model and reasoning effort. Give it only verified transcripts from the current workspace and task. The parent returns the result and does not mine or edit. If no native delegate or verified transcript source is available, report not-run and make no write. The isolated adapter in `scripts/cursor_native_hook_adapters.py` proves only threshold decisions on synthetic Codex Stop payloads; no hook is registered or trusted, and one bounded updater run on a synthetic transcript updated a fixture AGENTS.md and index, but real transcript scope and live host invocation remain unproven. This candidate remains unindexed.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Orchestrate continual learning by delegating transcript mining and AGENTS.md updates to `agents-memory-updater`.",
  "proof": "Pinned source hash and deterministic renderer preserve provenance. A bounded native memory curator reviewed one explicitly authorized transcript and produced a hashed proposal with no durable write; the mutating upstream role is not distributed.",
  "reason": "Bundle the upstream updater as a bounded native role and describe the actual synthetic hook proof without enabling the held workflow.",
  "replacements": [
    {
      "file": "SKILL.md",
      "before": "1. Call `agents-memory-updater`.",
      "after": "1. Delegate to the bundled [native `agents-memory-updater` role reference](references/agents-memory-updater.md) when native bounded delegation and a verified current-workspace transcript source are available; otherwise report not-run without writing."
    },
    {
      "file": "SKILL.md",
      "before": "- Do not bypass the subagent.",
      "after": "- Do not bypass the bounded native updater. An unavailable delegate is an explicit not-run result, not permission for the parent to mine transcripts."
    }
  ],
  "source_path": "continual-learning/skills/continual-learning/SKILL.md",
  "source_sha256": "f627f733a33879b63523da7e92c8b33e6086df42a49a8139a87d1c21e7bd9b16",
  "codex_native_body": "# Continual learning proposal for Codex\n\nUse this only when the user explicitly asks to mine supplied task history or when an operator-enabled trusted project workflow invokes it.\n\n## Workflow\n\n1. Verify the transcript is explicitly authorized, belongs to the current workspace/task scope, and excludes secrets and unrelated private conversations.\n2. Delegate the transcript review to one bounded native memory curator with an explicit proposal-only briefing. The coordinator supplies the exact input path and requests a proposal-only result.\n3. Ask the curator to identify durable candidate rules, cite their source lines or hashes, deduplicate them against current instructions, and distinguish user authority from temporary task wording.\n4. Return a reviewable proposal and receipt. Do not edit `AGENTS.md`, global memory, repository instructions, hooks, or runtime state unless the user separately authorizes that exact write.\n5. If the transcript or native delegate is unavailable, return `not-run` and leave all outputs unchanged.\n\nAutomatic Stop-hook triggering remains unavailable. The observed native proof reviewed one explicitly authorized local transcript, produced a hashed proposal, and performed no repository, instruction, memory, hook, or global-state write."
}
