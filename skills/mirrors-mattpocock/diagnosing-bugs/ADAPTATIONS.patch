--- a/diagnosing-bugs/SKILL.md
+++ b/diagnosing-bugs/SKILL.md
@@ -1,11 +1,11 @@
 ---
 name: diagnosing-bugs
-description: Diagnosis loop for hard bugs and performance regressions. Use when the user says "diagnose"/"debug this", or reports something broken/throwing/failing/slow.
+description: Diagnosis loop for hard bugs and performance regressions. Use when diagnosing a hard-to-reproduce bug or performance regression.
 ---
 
 # Diagnosing Bugs
 
-A discipline for hard bugs. Skip phases only when explicitly justified.
+A discipline for hard bugs. Adapt the phases to the evidence already available; explain any material verification gap.
 
 When exploring the codebase, read `CONTEXT.md` (if it exists) to get a clear mental model of the relevant modules, and check ADRs in the area you're touching.
 
@@ -17,9 +17,9 @@
 
 ## Phase 1: Build a feedback loop
 
-**This is the skill.** Everything else is mechanical. If you have a **tight** pass/fail signal for the bug (one that goes red on _this_ bug), you will find the cause; bisection, hypothesis-testing, and instrumentation all just consume it. If you don't have one, no amount of staring at code will save you.
+**This is the skill.** Everything else is mechanical. A pass/fail signal specific to the bug makes bisection, hypothesis testing and instrumentation more useful. Source inspection and provisional hypotheses can help construct that signal.
 
-Spend disproportionate effort here. **Be aggressive. Be creative. Refuse to give up.**
+Invest in a useful feedback loop in proportion to the difficulty and available access.
 
 ### Ways to construct one, in roughly this order
 
@@ -34,7 +34,7 @@
 9. **Differential loop.** Run the same input through old-version vs new-version (or two configs) and diff outputs.
 10. **HITL bash script.** Last resort. If a human must click, drive _them_ with `scripts/hitl-loop.template.sh` so the loop is still structured. Captured output feeds back to you.
 
-Build the right feedback loop, and the bug is 90% fixed.
+A representative feedback loop makes candidate fixes verifiable.
 
 ### Tighten the loop
 
@@ -48,11 +48,11 @@
 
 ### Non-deterministic bugs
 
-The goal is not a clean repro but a **higher reproduction rate**. Loop the trigger 100×, parallelise, add stress, narrow timing windows, inject sleeps. A 50%-flake bug is debuggable; 1% is not, so keep raising the rate until it's debuggable.
+The goal is not a clean repro but a **higher reproduction rate**. Loop the trigger 100×, parallelise, add stress, narrow timing windows, inject sleeps. Choose enough repetitions to distinguish the suspected failure from noise, within the available time and resources. Report uncertainty when the failure remains rare.
 
 ### When you genuinely cannot build a loop
 
-Stop and say so explicitly. List what you tried. Ask the user for: (a) access to whatever environment reproduces it, (b) a redacted captured artifact (HAR file, log dump, core dump, screen recording with timestamps), or (c) permission to add temporary production instrumentation. Do **not** proceed to hypothesise without a loop.
+Stop and say so explicitly. List what you tried. Ask the user for: (a) access to whatever environment reproduces it, (b) a redacted captured artifact (HAR file, log dump, core dump, screen recording with timestamps), or (c) permission to add temporary production instrumentation. You may continue read-only analysis with hypotheses labelled provisional. Do not present an unverified hypothesis as the cause or claim a fix is proven.
 
 ### Completion criterion: a tight loop that goes red
 
@@ -63,7 +63,7 @@
 - [ ] **Fast**: seconds, not minutes.
 - [ ] **Agent-runnable**: you can run it unattended; a human in the loop only via `scripts/hitl-loop.template.sh`.
 
-If you catch yourself reading code to build a theory before this command exists, **stop: jumping straight to a hypothesis is the exact failure this skill prevents.** No red-capable command, no Phase 2.
+Use code inspection to guide reproduction when needed. Distinguish a candidate explanation from a reproduced failure throughout the investigation.
 
 ## Phase 2: Reproduce + minimise
 
@@ -83,11 +83,11 @@
 
 Done when **every remaining element is load-bearing**: removing any one of them makes the loop go green.
 
-Do not proceed until you have reproduced **and** minimised.
+Minimise enough to isolate the relevant behaviour; continue when further minimisation would not improve the next diagnostic decision.
 
 ## Phase 3: Hypothesise
 
-Generate **3–5 ranked hypotheses** before testing any of them. Single-hypothesis generation anchors on the first plausible idea.
+When the cause is ambiguous, compare plausible hypotheses before committing to one. A clear diagnostic signal may justify testing a single explanation first.
 
 Each hypothesis must be **falsifiable**: state the prediction it makes.
 
