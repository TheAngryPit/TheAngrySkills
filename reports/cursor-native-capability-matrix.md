# Cursor → Codex capability proof matrix

State of draft PR #64 on 2026-09-14. A source hash or rendered
instruction is provenance, not execution proof. `held` means the skill is in
the 88-skill active inventory but outside the published skill catalog.

| Capability | Evidence in this branch | Remaining gap / decision |
| --- | --- | --- |
| Upstream catalog and source dependencies | Manifest pins 91 physical skills (88 active, 3 dormant), 27 plugin-level support files, and the native hook adapter by SHA-256. `sync-cursor-plugin-skills.py --check` reports 91 physical, 54 published, 167 output files. | 34 active candidates are held. Publication of the other 54 does not by itself prove runtime parity. |
| Installed pstack 47-skill comparison | [Per-skill audit](pstack-47-skill-audit-20260914.md) maps every top-level trigger, workflow, dependency, agent/model route, output, and gap. Installed cache and pin share all 47 IDs; 45 top-level skill bodies match byte-for-byte. Two skills and six nested playbooks differ; 27 cache-only support files are separated from the pin. | Installed cache says 0.15.1, but the skills-only pin lacks plugin release metadata. Three installed delegate defaults differ from the pin. No pin update or native activation follows from the comparison. |
| pstack playbooks and named companion skills | Held `cursor-poteto-mode` preview renders 23 playbooks and names `cursor-deslop`, `cursor-control-cli`, `cursor-control-ui`, model-capability-router, proof-orchestrator, and the public native `poteto-agent` profile. `cursor-no-comments` names `comment-sicko`; routed `how`, `why`, `interrogate`, `reflect`, and `swarm` retain their own roles. [Profile matrix](../docs/native-agent-profiles.md) records invocation and result ownership. The two source-grounded TOMLs match the installed global files byte-for-byte. | No fresh-session profile discovery/selection, sticky mode, cross-turn reuse, or named live reviewer/delegate execution proof. Companion skills and cloud-capable task lane are conditional on actual host availability. No agent Markdown is duplicated in the preview workbench. |
| `bug-fix` workflow | A synthetic notes app captures create/search, exact `not-found` failure, trace, and pass after a prewritten correction. It records a structural diff, commits local reproduction before correction, and pushes only to a temporary bare remote. The negative fixture refuses correction on a different failure. [Fixture report](../.scratch/pstack-bug-fix-notes/implementation-report.md). | `FIXTURE_ONLY`. Portions of source steps 1, 4, and 5 are exercised locally; 2, 3, and 6 are partial. No full `how`/`why` binary search, independent architect/delegation/review, real PR, or autonomously generated correction. |
| Project verification skills | A disposable notes CLI fixture runs each mapped action as a separate bounded Python subprocess, records exit code/stdout/stderr and declared regular-file state, then writes project-local `.agents/skills/verify-notes/` with Launch, Doctor, Drive, Evidence, Cleanup, Helpers, feature map, and surviving JSON evidence. A controlled feature drift is reconciled and a second unchanged pass is clean. `README` feature slugs are rejected before writing. | `FIXTURE_ONLY`: the generated Doctor checks only file existence/non-symlink, not Python syntax, version, or app health. No live target, source-wave delegation per feature, browser/Electron control, changed-outcome PR, native skill activation, or full maintenance pass is proven. Filesystem/network isolation was not observed. |
| Writing and TypeScript companion skills | Published `cursor-technical-writing` now requires the published `cursor-unslop` for every touched document, as the source does. Published `cursor-typescript-best-practices` names both `cursor-principle-type-system-discipline` and `cursor-principle-boundary-discipline` and retains bundled `references/patterns.md`. Generated-output tests cover the links. | These are corrected instruction links, not a live native trigger or combined writing/TypeScript workflow proof. |
| Official Cursor runtime contract | [Subagents](https://cursor.com/docs/subagents) document isolated local context, background/resume, inherited local MCP tools, and readonly write restrictions. [Cloud Agents API](https://cursor.com/docs/cloud-agent/api/endpoints) distinguishes agents/runs, repository refs, branches, artifacts and terminal cancellation. [Automations help](https://cursor.com/help/ai-features/automations) confirms an HTTP POST webhook trigger. | The pinned `why` claim that readonly removes MCP is a source assumption, not an official platform contract; its overlay now avoids granting write access to preserve MCP. Automation docs do not establish pstack's Bearer/`X-Automation-Key`, timeout, retry or response rules for `make-bot-ui`; the separate [Origin signed webhook](https://cursor.com/docs/api/origin) is a different protocol. Codex equivalence remains unproven. |
| pstack bundled scripts | [Static and scratch runtime review](../.scratch/pstack-bug-fix-notes/implementation-report.md) identifies Bun install, local store/locks, `git`/`gt`/`gh` subprocesses, `git fetch`, `gh api graphql`, and transcript-home reads. Exact lock pins installed in scratch with scripts disabled; a fresh cache-populated bootstrap installed and restarted. `orch` CLI changed a disposable store, `watch-pr` CLI parsed mocks and made one bounded read-only status query of PR #64, 52 bundled tests and typecheck passed, and a slow mock was stopped by an outer timeout. Node also ran `check-plan.mjs`; Bash ran `worktree-audit.sh` with mocks. | Polling/stack paths, frontier subprocesses, and live product behavior remain unproven. The bootstrap key does not verify the installed package version. The cache run was not network-instrumented; this is a limit of the proof, not a new gate. |
| `watch-pr` required review gate | The real PR #64 reported `REVIEW_REQUIRED`; the pinned source displayed Review ✅ because that cell only checked threads. A hash-checked held preview now renders `review required`, blocks non-draft `REVIEW_REQUIRED` as a merge gate, and retains `null` as a distinct no-decision state. Synthetic regression and a second read-only status query of PR #64 passed. | The preview is not distributed or trusted as a live Codex skill. The real PR is draft, so its Merge cell shows the draft gate; the non-draft merge gate is proven by the synthetic fixture, not by changing PR #64. |
| Ralph loop and cancel | Held previews bundle the hash-checked native adapter. Synthetic `Stop` payload tests cover exact promise, max iteration, duplicate turn, corrupt state, and task/project scoped cancellation. | No trusted `.codex/hooks.json`, live Stop event, or real loop continuation. |
| Advisor | Held preview bundles pinned role and native adapter. Synthetic payload tests cover known `apply_patch` marker, expected agent/verdict, one-time nudge, and explicit task-bound enable/status/expect/disable without default model. | No live read-only advisor consult or proved ordering for expected ID versus `SubagentStop`; shell/MCP/out-of-band edits are not sensed. |
| Continual learning | Held preview bundles pinned updater role and native adapter. Synthetic Stop tests cover threshold, transcript root/mtime, duplicate turn; a bounded synthetic updater pass changed only fixture AGENTS/index and its second unchanged-mtime pass was a no-op. | No real transcript, trusted hook, automatic update, or global memory write proof. |
| Agent compatibility | Held preview repairs pinned invalid YAML description and bundles four named reviewer roles; strict frontmatter audit has zero errors. | Published `agent-compatibility` scanner is not installed locally. Package/version/installation/egress review and real scanner, startup, validation, docs checks are absent; no combined score is claimed. |
| Documentation Canvas | The pinned `cursor-docs-canvas` source explicitly calls its own body a placeholder and requires `~/.cursor/skills-cursor/canvas/SKILL.md` plus Cursor Canvas SDK declarations. The candidate remains held. | No pinned Canvas SDK or proved native Codex canvas primitive maps that component/hook contract. A linked-document fallback is only a declared path, not Canvas parity. |
| Cursor plugin submission audit | Held preview bundles a hash-checked read-only structural auditor. Synthetic fixtures check manifest JSON, bounded component paths, frontmatter field presence, README, and passive hook/MCP detection. | Full YAML validity, marketplace source/name uniqueness, documentation quality, runtime behavior, and submission policy remain unproven. Source `needs_human_review` finding and `security_hold_*` promotion status remain intact; no recommendation to publish. |
| Cursor plugin scaffold | Held preview bundles a hash-checked static scaffold fixture plus pinned plugin-architect and quality-gate references. A marked disposable root produces a Cursor-format manifest, README, caller-supplied LICENSE and four static component examples; the held auditor returns `STRUCTURAL_PASS`. Hooks, `mcpServers`, and marketplace requests stop before writing. | The pinned source still defaults to `~/.cursor/plugins/local/`; the preview explicitly warns against following it. This is not a native Codex plugin installer, global installation, active component, or publication proof. Source security hold remains. |
| External service, cloud, hooks/MCP, and unsafe install surfaces | Manifest and per-skill overlays retain contextual scanner findings for xAI voice requests, Cursor SDK credential/MCP references, orchestrate scripts/Slack, create-plugin hooks/MCP, make-bot-ui install, and pstack scripts. | Held until each named service, secret, permission, side effect, and native host capability is reviewed and exercised. No global install, cloud task, secret submission, hook trust, or merge was performed. |

Validation for the state above: `pytest -q tests` passed 169 tests and 2
subtests; bundled Bun tests passed 52/52 and strict typecheck passed in scratch;
`sync-cursor-plugin-skills.py --check` and `git diff --check origin/main` passed.
GitHub `Validate skill stack` last passed on `7421f9e1`; CI for this new
integration head is pending publication.
