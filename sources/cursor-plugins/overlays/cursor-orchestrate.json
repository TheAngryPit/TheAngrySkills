{
  "codex_contract": {
    "schema_version": 1,
    "adapter_mode": "work-cloud-gated",
    "name_mapping": {
      "upstream_skill": "orchestrate",
      "published_name": "cursor-orchestrate",
      "source_path": "orchestrate/skills/orchestrate/SKILL.md"
    },
    "preserved_semantics": "Use only when the user explicitly types `/orchestrate <goal>` to decompose a large task, spawn a tree of parallel cloud-agent workers/subplanners/verifiers via the Cursor SDK, and collect structured handoffs; do not invoke autonomously.",
    "native_mapping": {
      "capabilities": [
        "explicit ChatGPT Work cloud task creation when a new task is requested",
        "native task list and readback",
        "bounded local plan and handoff validation"
      ],
      "required_tools": [
        "documented Work cloud task creation",
        "Work-compatible terminal status and handoff readback",
        "observed child dispatch, cancellation, recovery and artifact identity for full tree parity"
      ],
      "fallback": "When Work cloud dispatch or readback is unavailable, return a labeled unavailability and a reviewable plan; never present local delegates or Cursor SDK execution as Work cloud orchestration.",
      "availability": "work_cloud_create_contract_documented; tree_dispatch_drain_artifact_cancel_recover_unproven"
    },
    "external_dependencies": [
      "Native ChatGPT Work cloud task API and explicit new-task request for the selected destination",
      "Work-compatible child, status, handoff, artifact and recovery surface not yet observed",
      "Cursor SDK/API and CURSOR_API_KEY remain upstream-only dependencies, not the selected Work route",
      "Slack and Git/PR publication remain separate upstream side effects"
    ],
    "permission_gates": [
      "Preserve explicit-only invocation and verbatim user goal; create a user-owned Work cloud task only when the operator explicitly requests that new task and destination.",
      "Do not execute bundled Cursor scripts, request CURSOR_API_KEY, or substitute local subagents for selected Work cloud workers.",
      "A plan-only result is labeled as unavailable Work execution, not a completed orchestration.",
      "Contact Slack, push, merge, or publish only when separately authorized and proven.",
      "Respect the current task scope, selected model/effort, repository ownership, and explicit approval requirements."
    ],
    "behavioral_proof": {
      "positive": "An explicitly requested Work cloud root receives the verbatim goal, returns a ready task ID/URL, and a Work-compatible readback yields structured child handoffs and artifact identity before a combined report.",
      "missing_tool_or_input": "Absent goal, new-task authorization, Work-compatible drain, or child capability yields a labeled gap and reviewable plan without dispatch or local substitution.",
      "error": "Queued clientThreadId, failed child, missing artifact or recovery surface is reported with last confirmed state; no guessed wait, cancellation or merge."
    },
    "promotion_status": "held_until_behavior_proof_and_permission_gate",
    "upstream_readme": {
      "status": "verified_in_pinned_upstream_clone",
      "path": "orchestrate/README.md",
      "url": "https://github.com/cursor/plugins/blob/889ec4b68fa5aab0e867dad71ec3fdf386ae48f3/orchestrate/README.md",
      "note": "Verified in the pinned upstream clone; the README is referenced for provenance and is not vendored in this skills-only snapshot."
    },
    "security_review": {
      "scanner_verdict": "blocked_malicious",
      "status": "retained_contextual_hold",
      "decision": "Hold. Keep the bundled runtime external and require a separate code/security review before admission. Hold with the bundled runtime; do not install dependencies from this mirror. Hold until the command boundary, environment allowlist, timeout, and cleanup receive separate review. Retain the finding as contextual/defensive, but hold the bundled runtime until the policy test and implementation are reviewed together. Retain the scanner finding and hold the bundled Slack adapter pending external-egress and side-effect review.",
      "findings": [
        {
          "rule_id": "bundled-script-review",
          "severity": "warning",
          "path": "scripts/**",
          "line": null,
          "count": 62,
          "classification": "bundled_executable_surface",
          "effect": "The skill ships TypeScript/Bun scripts, tests, adapters, and CLI code that can execute local and external workflow actions.",
          "credentials": [
            "CURSOR_API_KEY",
            "SLACK_BOT_TOKEN"
          ],
          "decision": "Hold. Keep the bundled runtime external and require a separate code/security review before admission."
        },
        {
          "rule_id": "dependency-manifest-surface",
          "severity": "warning",
          "path": "scripts/package.json",
          "line": null,
          "classification": "active_dependency_surface",
          "effect": "The package manifest can install and run bundled dependencies.",
          "credentials": [],
          "decision": "Hold with the bundled runtime; do not install dependencies from this mirror."
        },
        {
          "rule_id": "persistence-mutation",
          "severity": "critical",
          "path": "scripts/measurements.ts",
          "line": 302,
          "classification": "active_executable_code_with_security_boundary",
          "effect": "runMeasurementCommand executes planner-authored bash in a worker branch and creates/removes a scratch HOME. The allowlist and cleanup are defensive controls, but the shell boundary is still active.",
          "credentials": [
            "allowlisted process environment"
          ],
          "decision": "Hold until the command boundary, environment allowlist, timeout, and cleanup receive separate review."
        },
        {
          "rule_id": "policy-bypass-instruction",
          "severity": "critical",
          "path": "scripts/__tests__/operator-boundary.test.ts",
          "line": 38,
          "classification": "defensive_test_reference",
          "effect": "The test asserts that ORCHESTRATE_OPERATOR is ignored without the home flag. The scanner matches the word \"ignores\" although the test enforces the policy gate.",
          "credentials": [],
          "decision": "Retain the finding as contextual/defensive, but hold the bundled runtime until the policy test and implementation are reviewed together."
        },
        {
          "rule_id": "persistence-mutation",
          "severity": "critical",
          "path": "scripts/adapters/slack/index.ts",
          "line": 51,
          "classification": "read_only_false_positive_context_with_external_adapter",
          "effect": "The flagged line only reads a Slack profile field; it is not persistence. The surrounding adapter still provides Slack API access and can create external messages elsewhere.",
          "credentials": [
            "SLACK_BOT_TOKEN"
          ],
          "decision": "Retain the scanner finding and hold the bundled Slack adapter pending external-egress and side-effect review."
        }
      ]
    }
  },
  "codex_note": "## Codex Work cloud mapping and security hold\n\nThe operator selected ChatGPT Work cloud as the Codex destination. This candidate remains unindexed. The documented native API can create a user-owned Work task on an explicit new-task request; it does not yet prove the pinned source's tree dispatch, drain, structured handoffs, Git artifacts, cancellation or recovery. Read reports/cursor-orchestrate-work-cloud-contract-20260914.md before any admission. A plan-only response must label Work execution unavailable; local subagents and Cursor SDK scripts are not substitutes. Finding review: bundled-script-review at scripts/**:n/a (bundled_executable_surface); dependency-manifest-surface at scripts/package.json:n/a (active_dependency_surface); persistence-mutation at scripts/measurements.ts:302 (active_executable_code_with_security_boundary); policy-bypass-instruction at scripts/__tests__/operator-boundary.test.ts:38 (defensive_test_reference); persistence-mutation at scripts/adapters/slack/index.ts:51 (read_only_false_positive_context_with_external_adapter). Keep the bundled runtime external and do not install dependencies from this mirror. Retain the scanner findings for the upstream Cursor runtime; any Work-native implementation needs its own behavior and permission proof.\n",
  "codex_rule": "Respect native Codex model/effort, permissions and actual tool availability; no automatic Cursor runtime equivalence.",
  "preserved_requirement": "Use only when the user explicitly types `/orchestrate <goal>` to decompose a large task, spawn a tree of parallel cloud-agent workers/subplanners/verifiers via the Cursor SDK, and collect structured handoffs; do not invoke autonomously.",
  "proof": "Pinned source hash and deterministic build are static provenance. The native Work cloud API contract and exact missing operations are mapped in reports/cursor-orchestrate-work-cloud-contract-20260914.md; no Work tree ran. Contextual upstream security findings and hold decisions remain recorded.",
  "reason": "Map the operator-selected ChatGPT Work cloud destination without silently running Cursor SDK scripts or relabeling local delegation as cloud parity. Preserve the source/security provenance and keep this skill unindexed until Work behavior is proved.",
  "replacements": [],
  "source_path": "orchestrate/skills/orchestrate/SKILL.md",
  "source_sha256": "a56718f3acf4bbc13e44ca7b8e00d499e818fb16ee293bbe3d58621968c28e22"
}
